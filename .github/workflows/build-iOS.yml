name: Build-iOS
on:
  pull_request:
    paths:
    - 'iOS/**'
    - '.github/workflows/build-iOS.yml'
jobs:
  test:
    name: Build and test
    runs-on: macOS-latest
    strategy:
        matrix:
          destination: ['platform=iOS Simulator,OS=13.7,name=iPhone 11']
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Force XCode 12.5
        run: sudo xcode-select -switch /Applications/Xcode_12.5.app
      - name: Build
        run: |
          cd iOS/MyStudies
          xcodebuild clean build -workspace MyStudies.xcworkspace -scheme MyStudies -destination "${destination}" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=YES EXCLUDED_ARCHS[sdk=iphonesimulator*]=arm64
        env: 
         destination: ${{ matrix.destination }}
      - name: Test
        run: |
          cd iOS/MyStudies
          xcodebuild test -workspace MyStudies.xcworkspace -scheme MyStudies -destination "${destination}"
        env: 
         destination: ${{ matrix.destination }}
