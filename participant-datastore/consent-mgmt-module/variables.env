# Application properties configuration for user-mgmt-module
# These environment variables are used by the application.properties file to configure deployment behavior

# Path to the directory within the container where this application will write log files. Example value below.
LOG_PATH=/logs

# URL of your Participant datastore database. Example value below.
DB_INSTANCE_URL=10.112.224.10

# Name of the database that you created with the sql script. Default value is ‘mystudies_participant_datastore’.
DB_NAME=mystudies_participant_datastore

# Username of the account that you created for the database. Example value below.
DB_USER=participant-datastore

# Password of the account that you created for the database. Example value below.
DB_PASS=participant-datastore

# The username for the email account that the platform will use to send emails. Example value below.
FROM_EMAIL_ADDRESS=mystudies.test.1@gmail.com

# The password for the email account that the platform emails. Example value below.
FROM_EMAIL_PASSWORD=

# The address of the SMTP server that the platform will use to send emails. Example value below.
SMTP_HOSTNAME=smpt.gmail.com

# Default value is ‘False’, which causes the Participant datastore to authenticate with the from_email_address and from_email_password - if ‘True’, the platform will not authenticate and will rely on the allowlist configured in the SMTP service
SMTP_USE_IP_ALLOWLIST=False

# Domain should be the email domain excluding the end, i.e. domain instead of domain.com. Example value below.
FROM_EMAIL_DOMAIN=gmail

# URL of your Firebase Cloud Messaging API.
FCM_API_URL=

# Set to “sandbox” or “production” based on your iOS app release type
IOS_NOTIFICATION_TYPE=production

# URL of your Auth server instance, including context-path value (e.g. /auth-server). Example value below.
SCIM_AUTH_URL=http://10.128.0.3/auth-server

# URL of your Response datastore instance, including  context-path value (e.g. /response-datastore). Example value below.
RESPONSE_DATASTORE_URL=http://10.128.0.8/response-datastore

# Email address where participant submissions to the mobile application “feedback” and “contact” forms will be sent
MAIL_CONTACT_EMAIL=mystudies.test.1@gmail.com

# The display name of the deploying organization, for example ‘Sincerely,  the <<org_name>> support team’
ORG_NAME=MyStudies

# Client id that you set in your Hydra instance for the user-mgmt-module. See Hydra documentation for more information. 
CLIENT_ID=clientid_single
 
# Client secret that you set in your Hydra instance for the user-mgmt-module. See Hydra documentation for more information.
SECRET_KEY=clientsecret_single

# The administrative URL of your Hydra instance. Typically configured on port 4445. Example value below.
HYDRA_ADMIN_URL=https://10.128.0.2:4445

# Username of the SMTP account that the platform will use to send emails
MAIL_USERNAME=mystudies.test.1@gmail.com

# Password of the SMTP account that the platform will use to send emails
MAIL_PASSWORD=

# The contact email address presented to users. Example value below.
MAIL_CONTACT_EMAIL=mystudies.test.1@gmail.com
 
# The return email address for messages sent by the platform. Example value below.
MAIL_FROM_EMAIL=noreply@fdamystudies.net
