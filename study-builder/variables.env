# Application properties configuration for `Study builder`
# These environment variables are used by the application.properties file to configure deployment behavior

# URL of your `Study datastore` database (configured during `Study datastore` deployment). Example value below.
DB_INSTANCE_URL=10.112.224.8

# Name of the database that you created with the sql script during `Study datastore` deployment. Default value is `fda_hphc`.
DB_NAME=fda_hphc

# Username of the account that you created for the database. Example value below.
DB_USER=study-builder

# Password of the account that you created for the database. Example value below.
DB_PASS=study-builder

# Base URL of this `Study builder` instance. Example value below.
BASE_URL=http://10.128.0.4

# URL of your `Participant datastore` user-mgmt-module with context path (e.g. /participant-user-datastore). Example value below. 
PARTICIPANT_USER_DATASTORE_URL=http://10.128.0.7:8080/participant-user-datastore

# URL of your `Response datastore` instance with context path (e.g. /response-datastore). Example value below. 
RESPONSE_DATASTORE_URL=http://10.128.0.8/response-datastore

# Client id that you set in your Hydra instance for the `Study builder`. See Hydra documentation for more information. 
CLIENT_ID=clientid_single
 
# Client secret that you set in your Hydra instance for the `Study builder`. See Hydra documentation for more information.
SECRET_KEY=clientsecret_single

# URL of your Auth server instance, including context-path value (e.g. /auth-server). Example value below.
SCIM_AUTH_URL=http://10.128.0.3/auth-server

# Name of Google Cloud Storage bucket that you created to store study resources.
GCP_BUCKET_NAME=study-builder-resources

# The username for the email account that the platform will use to send emails. Example value below.
FROM_EMAIL_ADDRESS=mystudies.test.1@gmail.com

# The password for the email account that the platform emails. Example value below.
FROM_EMAIL_PASSWORD=xxxxxx

# The address of the SMTP server that the platform will use to send emails. Example value below.
SMTP_HOSTNAME=smpt.gmail.com

# Default value is ‘False’, which causes the `Study builder` to authenticate with the from_email_address and from_email_password - if ‘True’, the platform will not authenticate and will rely on the allowlist configured in the SMTP service
SMTP_USE_IP_ALLOWLIST=False

# Domain should be the email domain excluding the end, i.e. domain instead of domain.com. Example value below.
FROM_EMAIL_DOMAIN=gmail
